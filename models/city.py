#!/usr/bin/python3

from models.base_model import BaseModel

"""Defines a city class"""
class City(BaseModel):
    """Representatin of a class city
    Attributes: 
    state_id (str): Initial empty string of the state's id
    name (str): Initial empty string of the state's name
    """
    state_id = " "
    name = " "